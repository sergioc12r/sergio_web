import 'package:flutter/material.dart';

class SendMeMessageForm extends StatelessWidget {
  const SendMeMessageForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
