import 'package:flutter/material.dart';

/// Model to manage app bar action buttons and tap functions
class AppBarActionModel {
  final String title;
  final Widget child;

  const AppBarActionModel({required this.title, required this.child});
}
